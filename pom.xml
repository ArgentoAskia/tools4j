<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cn.argento.askia</groupId>
    <artifactId>cocoa-java</artifactId>
    <packaging>pom</packaging>
    <version>0.0.1-alpha</version>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>8</source>
                    <target>8</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <modules>
        <module>cocoa-utilities</module>
        <module>cocoa-annotations</module>
        <module>cocoa-design-patterns</module>
        <module>cocoa-command-tools-providers</module>
        <module>cocoa-language-extends</module>
        <module>cocoa-concurrent</module>
        <module>cocoa-core</module>
        <module>cocoa-gui</module>
        <module>cocoa-network</module>
    </modules>

    <!--
        project.build.sourceEncoding：控制编译 Java 源码、复制资源文件时使用的字符编码。
        project.reporting.outputEncoding：控制生成报告（如 Javadoc、Surefire 报告）时的输出编码。
    -->
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>


    <dependencyManagement>
        <!-- 引入自身模块方便其他子模块引入 -->
        <dependencies>
            <dependency>
                <groupId>cn.argento.askia</groupId>
                <artifactId>cocoa-annotations</artifactId>
                <version>0.0.1-alpha</version>
            </dependency>
            <dependency>
                <groupId>cn.argento.askia</groupId>
                <artifactId>cocoa-design-patterns</artifactId>
                <version>0.0.1-alpha</version>
            </dependency>
            <dependency>
                <groupId>cn.argento.askia</groupId>
                <artifactId>cocoa-utilities</artifactId>
                <version>0.0.1-alpha</version>
            </dependency>
            <dependency>
                <groupId>cn.argento.askia</groupId>
                <artifactId>cocoa-core</artifactId>
                <version>0.0.1-alpha</version>
            </dependency>
        </dependencies>
    </dependencyManagement>


    <!-- 所有模块都需要的依赖 -->
    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>





</project>